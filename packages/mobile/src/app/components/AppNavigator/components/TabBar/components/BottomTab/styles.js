import { black, emperor } from '@constants/colors';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  iconFocus: {
    tintColor: black
  },
  icon: {
    tintColor: emperor
  },
  focus: {
    color: black
  }
});
